#include "holberton.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: Number one to be multiply
 * @b: Number two to be multiply
 * Return: mul
 */
int mul(int a, int b)
{
  int y;
  y = a*b;
  return (y);
}
